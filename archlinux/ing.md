
``` shell
$ pacman -S dialog wpa_supplicant

# wpa enterprise
http://growworkinghard.altervista.org/netctl-wifi-menu-with-wpa2-enterprise-network/

$ sudo -u gdm dbus-launch gsettings set org.gnome.desktop.interface scaling-factor 1


```

``` shell
# desktop environment
$ pacman -S lxdm openbox tint2 urxvt skippy-xd dunst

$ pacman -S emacs 
```
